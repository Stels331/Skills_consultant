# Principles Library (Sprint S1-T4)

Each file defines one principle that can be injected into semantic validation context.

Fields:
- `principle_id`
- `title`
- `scope_stages` (comma-separated)
- `description`
- `checklist`
