# S2-T3 — Canonical case model + index + versioning base (глубокое описание)

## Мета
- Сформульована у задачі та узгоджена з цілями спринта.

## Що робимо
- Виконуємо обсяг робіт, описаний у цій задачі (див. робочі деталі нижче).

## Як виконання
- Реалізація по кроках із фіксацією артефактів у файловій структурі проекту.

## Очікуваний результат
- Отримано завершений артефакт задачі, готовий для інтеграції в потік спринта.

## Як тестувати виконання
- Запустити перевірки/скрипти модуля задачі локально в IDE.
- Перевірити, що всі вихідні файли задачі створені/оновлені коректно.
- Перевірити, що зміни не ламають попередній workflow спринта.

## Контрольні точки поза тестуванням
- Актуальність документації та читабельність артефактів для handoff.
- Відповідність архітектурним правилам (problem-first, evidence-first, no-DB v1).
- Відмітка прогресу задачі та синхронізація зі статусом спринта.

## Робочі деталі (початковий опис задачі)

**Приоритет:** Should


## Scope
- Интеграция extraction, typing, characterization в единую модель.
- Поддержка ссылочной целостности entity/claim/relation IDs.
- Версионирование ключевых изменений состояния.

## Технические шаги
1. Реализовать model builder с merge-логикой.
2. Построить `model_index.json` (обратные индексы по типам/ID/связям).
3. Добавить `create_checkpoint()` и хранение snapshot в `versions/vN/`.
4. Логировать diff-метаданные в `version_changelog.json`.

## Нефункциональные требования
- Оптимизация под selective loading.
- Устойчивость к частичным обновлениям.
- Только файловое хранение; БД отсутствует.

## Acceptance
- Индекс покрывает все ключевые сущности модели.
- Создание checkpoint работает автоматически на структурных изменениях.
